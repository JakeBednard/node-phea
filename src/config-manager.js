const readlineSync = require('readline-sync');

function getConfig(options) {

    // check if options.configFile file exist

    // else check the default directory

    // else prompt user to config Hue bridge

}